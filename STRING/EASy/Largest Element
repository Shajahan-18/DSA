class Solution {
    public int largestElement(int[] nums) {
        int large=Integer.MIN_VALUE;
        for(int i=0;i<nums.length;i++){
            if(nums[i]>large){
               large=nums[i];
            }
        
        }
     return large;
    }
}
